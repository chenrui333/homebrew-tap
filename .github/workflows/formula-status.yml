name: Formula Metadata

on:
  # Run every Monday at 09:00 UTC
  schedule:
    - cron: '0 9 * * 1'
  # Allow manual triggering
  workflow_dispatch:

jobs:
  generate-metadata:
    runs-on: macos-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - name: Set up Homebrew
        uses: Homebrew/actions/setup-homebrew@master

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Verify dependencies
        run: |
          which python3
          which gh
          python3 --version
          gh --version

      - name: Configure Git
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

      - name: Generate formula metadata
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          python3 scripts/generate_formula_status.py --output STATUS.md

      - name: Check for changes
        id: check_changes
        run: |
          if git diff --quiet STATUS.md .cache/; then
            echo "changed=false" >> $GITHUB_OUTPUT
            echo "No changes detected"
          else
            echo "changed=true" >> $GITHUB_OUTPUT
            echo "Changes detected"
          fi

      - name: Commit and push changes
        if: steps.check_changes.outputs.changed == 'true'
        run: |
          git add STATUS.md .cache/
          git commit -m "chore: update formula metadata

          Generated by GitHub Actions on $(date -u +"%Y-%m-%d %H:%M:%S UTC")

          [skip ci]"
          git push origin ${{ github.ref_name }}

      - name: Summary
        run: |
          if [ -f STATUS.md ]; then
            echo "## Formula Metadata Report" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "✅ Metadata report generated successfully" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY

            TOTAL=$(grep -c '^| [a-z]' STATUS.md 2>/dev/null || echo 0)
            echo "**Total formulas**: $TOTAL" >> $GITHUB_STEP_SUMMARY

            if [ "${{ steps.check_changes.outputs.changed }}" = "true" ]; then
              echo "" >> $GITHUB_STEP_SUMMARY
              echo "📝 Changes committed and pushed" >> $GITHUB_STEP_SUMMARY
            else
              echo "" >> $GITHUB_STEP_SUMMARY
              echo "ℹ️ No changes detected" >> $GITHUB_STEP_SUMMARY
            fi
          else
            echo "❌ Failed to generate metadata report" >> $GITHUB_STEP_SUMMARY
          fi
